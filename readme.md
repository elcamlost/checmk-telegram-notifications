# Todo #
- add logging to see what the script is actually doing
- make configurable when to include graphs (e.g. do not send graphs for OK notifications)
- make host and service template configurable
- consider changing the formatting of notifications
